# Uncomment the next line to define a global platform for your project
platform :ios, '11.3'

target 'Satisfy' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Satisfy
  pod 'Charts'
  pod 'SwiftGifOrigin'
  pod 'SwiftyJSON'

  target 'SatisfyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SatisfyUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
